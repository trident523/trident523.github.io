source "https://rubygems.org"

# GitHub Pages builds this site automatically using its pinned gem set.
# This Gemfile is only needed if you want to run `bundle exec jekyll serve`
# locally — it's optional.
gem "github-pages", group: :jekyll_plugins
